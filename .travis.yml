language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-iced-react-webpack' ]; then cd .. && eval "mv $currentfolder generator-iced-react-webpack" && cd generator-iced-react-webpack; fi

